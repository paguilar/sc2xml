/*
 * test3.stub.h: Needed for pre-processing test3.h, otherwise we cannot
 * parse the macro. The C pre-processor generates the header file called 
 * test3.gen.h that will be parsed and, if no errrors occur, it
 * will generate the XML file test3.h.xml
 */

#include "test3.h"

def_struct_data(prefix,number,code_number)

